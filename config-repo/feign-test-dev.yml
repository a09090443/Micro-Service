properties:
  project: feign-test
  env: dev
  log-level: INFO

server:
    session:
      cookie:
        name: UISESSION

ribbon:
  ReadTimeout:  30000
  ConnectTimeout:  15000
  eureka:
    enabled: true

feign:
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000
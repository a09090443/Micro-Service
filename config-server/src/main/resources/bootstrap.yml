spring:
  application:
    name: config-server 
  cloud:
    config:
      server:
        git:
          uri: ${config.repo.uri:https://github.com/a09090443/Micro-Service}
          searchPaths: config-repo
#         username:
#         password:
server:
  port: 8888
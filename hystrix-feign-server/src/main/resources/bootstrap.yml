spring:
  application:
    name: hystrix-feign-server
  cloud:
    config:
      profile: dev
      uri: http://192.168.1.154:8000
      label: develop

feign:
  hystrix:
    enabled: true

server:
  port: 8101
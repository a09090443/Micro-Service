spring:
  application:
    name: oauth-jdbc-client
  profiles:
    active: dev, pro
  cloud:
    config:
#      profile: dev
      uri: http://127.0.0.1:8888
      label: develop

server:
  port: 8082